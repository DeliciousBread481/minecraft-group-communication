<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.konstantyn111.crashapi.mapper.announcement.AnnouncementCategoryMapper">

    <resultMap id="CategoryMap" type="com.github.konstantyn111.crashapi.entity.announcement.AnnouncementCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAllOrderBySortOrder" resultMap="CategoryMap">
        SELECT *
        FROM announcement_categories
        ORDER BY sort_order
    </select>

    <select id="findById" resultMap="CategoryMap">
        SELECT * FROM announcement_categories WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO announcement_categories
            (id, name, sort_order, created_at)
        VALUES
            (#{id}, #{name}, #{sortOrder}, #{createdAt})
    </insert>

    <update id="update">
        UPDATE announcement_categories
        SET
            name = #{name},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM announcement_categories WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM announcement_categories
    </select>
</mapper>